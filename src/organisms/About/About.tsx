import { Box } from "@mui/system";

import styles from "./About.module.scss";

export const About = () => {
  return (
    <div>
      <Box className={styles.about}>
        <h1>about</h1>
      </Box>
    </div>
  );
};
